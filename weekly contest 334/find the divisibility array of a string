class Solution {
public:
    vector<int> divisibilityArray(string word, int m) {
 long t;
 vector<int> ans;
 for(int i=0;i<word.size();i++){
     t=t*10+(word[i]-'0');
     t%=m;
     if(t==0){
ans.push_back(1);
else
ans.push_back(0);
     }
     return ans;
};
